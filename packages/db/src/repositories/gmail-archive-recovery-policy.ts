/** Fixed DB-clock grace before an in-progress archive attempt is recovery-eligible. */
export const GMAIL_ARCHIVE_RECOVERY_GRACE_SECONDS = 300;

/** One-shot wall-clock budget for the complete recovery observation envelope. */
export const GMAIL_ARCHIVE_RECOVERY_OBSERVATION_DEADLINE_SECONDS = 150;
